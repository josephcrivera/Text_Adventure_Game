/*Class CommandWords *  * This class holds an enumeration of all command words known to the game. * It is used to recognize commands as they are typed in. * */class CommandWords{    // a constant array that holds all valid command words    // just list all of the possible commands here    private static final String validCommands[] = {        "go", "quit", "help", "inventory", "get", "examine", "use", "answer"    };    /**     * Constructor - initialize the command words.     */    public CommandWords()    {        // nothing to do at the moment...    }    /**     * Check whether a given String is a valid command word.     * Loop through validCommands, searching for the string     * that was inputted.  If you find it return true.  Otherwise     * after the loop return false.      * Return true if it is, false if it isn't.     **/    public boolean isCommand(String aString)    {        for(int i = 0; i < validCommands.length; i++)        {            if(validCommands[i].equals(aString))                return true;        }        // if we get here, the string was not found in the commands        return false;    }    /*     * Print all valid commands to System.out.     */    public void showAll()     {        for(int i = 0; i < validCommands.length; i++)        {            System.out.print(validCommands[i] + "  ");        }        System.out.println();    }}